{
  "version": "0.2.0",
  "configurations": [
    {
      "name": "Abrir con Chrome (Live Server)",
      "type": "chrome",
      "request": "launch",
      "url": "http://localhost:5500",
      "webRoot": "${workspaceFolder}",
      "preLaunchTask": "Abrir Live Server"
    }
  ]
}
